<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apis.amapv2.com.canvas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CanvasDrawXXXActivity"/>
        <activity android:name=".activity.CanvasChangeActivity" />
        <activity android:name=".activity.CanvasSaveRestoreActivity" />
        <activity android:name=".activity.CanvasTranslateActivity" />
        <activity android:name=".activity.CanvasScaleActivity"></activity>
    </application>

</manifest>